# 前端便签 - API连接问题排查 (更新)

**日期**: 2025-01-27  
**发送方**: 前端团队  
**接收方**: 后端团队  

## 🔍 当前状态

### ✅ 已解决问题
1. **环境配置**: .env.local 已正确配置
   ```
   NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
   NEXT_PUBLIC_WS_ORCHESTRATION_URL=http://localhost:4000
   NEXT_PUBLIC_WS_ORCHESTRATION_PATH=/ws/orchestration
   ```

2. **URL构建问题**: 已修复 `apiClient.ts` 中的路径拼接逻辑
   - 解决了双路径问题 (`/api/v1/api/v1/medicines`)
   - 修复了相对路径处理

3. **代码变量作用域**: 修复了 `prescriptionService.ts` 中的变量声明问题

### 🔄 前端服务状态
- **端口**: 3000 (正常运行)
- **编译**: 成功，无 TypeScript 错误
- **Mock数据验证**: 全部通过 (15/15)

## ❌ 待解决问题 (更新)

### 1. 处方创建 API 返回 400 错误 (数据验证失败)
**错误信息 (来自浏览器控制台)**:
- `【处方服务】处方创建失败: {...AxiosError: Request failed with status code 400...}`
- `【处方服务】数据验证失败，请检查数据格式`
- `处方保存失败: {...Error: 数据格式错误：请确保所有必填字段完整，特别是clinicId和medicines[].dosageInstructions...}`

**请求URL**: `POST http://localhost:4000/api/v1/prescriptions`  

**前端发送的请求数据示例**:
```json
{
  "clinicId": "default_clinic_id",
  "patientInfo": {
    "name": "测试患者",
    "age": 30,
    "gender": "male"
  },
  "medicines": [
    {
      "medicineId": "cmc1bzn21000pugr4luvxa52o",
      "quantity": 15,
      "dosageInstructions": "水煎服，每次1剂，每日2次，温服"
    }
  ],
  "notes": ""
}
```

### 2. 需要后端团队确认的问题 (更具体化)

#### 🔧 数据验证问题 (急需确认)
1. **`clinicId` 字段**:
   - 当前前端仍然使用 `"default_clinic_id"`。
   - **请求**: 请提供后端接受的**有效 `clinicId` 值列表**，或说明获取这些 `clinicId` 的**具体 API 或机制**。
2. **`medicines[].dosageInstructions` 字段**:
   - 前端已发送包含中文说明的 `dosageInstructions` (例如："水煎服，每次1剂，每日2次，温服")。
   - **请求**: 请提供 `dosageInstructions` 字段**确切的验证规则和期望的数据格式** (例如：是否为固定枚举值、允许的最大长度、是否支持中文、特殊字符限制等)。
3. **详细验证错误信息**:
   - 当前 400 响应只返回"Validation failed"或"数据格式错误"，**缺乏具体字段的错误详情**。
   - **强烈请求**: 能否在 400 响应中提供**更详细、字段级别的验证错误信息**，例如：
   ```json
   {
     "error": "Validation failed",
     "details": [
       {
         "field": "clinicId",
         "message": "Invalid clinic ID: 'default_clinic_id' is not allowed."
       },
       {
         "field": "medicines[0].dosageInstructions",
         "message": "Dosage instructions format is incorrect. Expected: Enum values."
       }
     ]
   }
   ```
   这将极大帮助前端快速定位并修复问题。

#### 🔐 认证与权限问题 (再次确认)
1. **Token 有效性**:
   - **请求**: 请再次确认当前登录用户的 JWT token 是否有效且未过期。前端将尝试重新登录以获取新 token。
2. **权限验证**:
   - **请求**: 请确认当前登录用户角色 (例如: 医生) 是否具有调用 `/api/v1/prescriptions` 接口创建处方的权限。

#### 📋 API 文档一致性
1. **Swagger 文档**:
   - **请求**: 请确认后端 Swagger 文档 (期望地址 `http://localhost:4000/api/docs`) 是否是最新的，并与当前后端逻辑完全一致，特别是 `CreatePrescriptionDto` 的结构及其所有字段的验证规则。

## 🔄 建议的调试步骤

### 后端团队请协助：
1. **检查后端服务状态**: 确认 API 服务在 4000 端口和数据库连接正常。
2. **验证处方创建端点**: 使用 Postman/curl 带着有效数据和 token 测试 `/api/v1/prescriptions` 端点，并提供一个**成功的请求示例**。
3. **药品数据确认**: 检查药品搜索 API (`/api/v1/medicines`) 返回的 `medicineId` 是否能在创建处方时有效使用。
4. **提供验证规则**: 明确提供 `clinicId` 和 `medicines[].dosageInstructions` 的完整验证规则和期望格式。

### 前端团队将执行：
1. **重新登录**: 获取新的认证 token。
2. **使用真实数据**: 在后端提供有效 `clinicId` 和 `dosageInstructions` 格式后，使用实际搜索到的 `medicineId` 和符合格式的数据再次尝试创建处方。
3. **详细日志**: 持续提供完整的请求/响应日志，包括 HTTP 状态码和响应体。

## 📞 联系方式
如有疑问，请及时沟通。我们可以安排实时调试会议。

---
**下一步**: 等待后端团队反馈后，前端将根据提供的信息进行相应调整。 