---
description: 
globs: 
alwaysApply: true
---
# CI/CD与部署规范

## 1. 持续集成 (CI)
- **自动化测试**：每次代码提交都应自动触发单元测试和集成测试。
- **代码 Linting**：强制执行代码风格检查，确保代码质量和一致性。
- **类型检查**：对TypeScript代码进行类型检查，捕获编译期错误。
- **构建产物**：成功通过CI后，生成可部署的Docker镜像。
- **快速反馈**：确保CI流水线运行速度快，提供及时反馈。

## 2. 持续部署 (CD)
- **环境区分**：明确开发（Dev）、测试（Staging）、生产（Production）环境的部署流程和触发条件。
- **自动化部署**：通过CI/CD流水线实现自动化部署到各个环境。
- **回滚机制**：确保部署失败时可以快速回滚到上一个稳定版本。
- **零停机部署**：可能的情况下，采用蓝绿部署或滚动更新策略，减少停机时间。

## 3. Docker 化
- **Dockerfile 编写**：编写清晰、高效的 `Dockerfile`，优化镜像大小和构建速度。
- **多阶段构建**：使用多阶段构建减少最终镜像大小，提高安全性。
- **环境变量**：通过Docker环境变量配置应用，而非硬编码。

## 4. 数据库迁移**
- 在CI/CD流程中，确保数据库迁移（`npx prisma migrate deploy`）在应用部署之前执行。
- 确保迁移脚本的幂等性，防止重复执行导致问题。

## 5. 监控与告警
- 集成应用性能监控（APM）工具（如DataDog, Prometheus + Grafana）来监控后端服务。
- 配置关键指标的告警（例如：错误率、响应时间、CPU/内存使用率）。

## 6. 版本控制
- 遵循Git Flow或GitHub Flow等版本控制策略。
- 明确主分支（`main`）、开发分支（`develop`）和功能分支（`feature/*`）的用途。
