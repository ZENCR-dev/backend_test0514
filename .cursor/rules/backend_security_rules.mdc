---
description: 
globs: 
alwaysApply: true
---
# 后端安全开发实践

## 1. 输入验证与净化
- 对所有来自客户端的输入进行严格的验证和净化，防止注入攻击（SQL注入、NoSQL注入、命令注入等）。
- 使用 `class-validator` 等库进行DTO层面的验证。
- 对敏感输入进行适当的编码或转义。

## 2. 认证与授权
- **认证**：使用健壮的认证机制（JWTs），并确保Token的安全存储、传输和刷新。
- **授权**：实现基于角色的访问控制（RBAC），确保用户只能访问其被授权的资源和操作。
- 在API层面使用NestJS的Guard进行权限检查。
- 结合Supabase的Row Level Security (RLS) 形成纵深防御。

## 3. 数据安全与隐私
- **敏感数据加密**：对存储在数据库中的敏感数据（如用户密码、身份证号、医疗记录）进行应用层加密或使用数据库加密功能。
- **数据传输加密**：强制使用HTTPS/SSL进行所有数据传输。
- **日志安全**：避免在日志中记录敏感信息。

## 4. 依赖项安全
- 定期审计和更新所有第三方依赖库，修复已知漏洞。
- 订阅安全公告，及时响应新的漏洞。

## 5. API 安全
- **速率限制 (Rate Limiting)**：防止暴力破解、DDoS攻击。
- **CORS 配置**：严格配置CORS策略，只允许受信任的域名访问。
- **安全头 (Security Headers)**：配置HTTP安全头（如 `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`）。
- **错误信息暴露**：在生产环境中，避免在错误响应中暴露敏感的系统信息（如堆栈跟踪）。

## 6. 身份验证凭证管理
- 密钥（Secrets）和API凭证不应硬编码在代码中，应通过环境变量或安全的配置管理系统加载。
- 定期轮换API密钥和数据库密码。

## 7. 文件上传安全
- 对上传的文件进行类型、大小和内容检查，防止恶意文件上传。
- 使用预签名URL（如Supabase Storage的）控制文件访问权限和有效期。
- 将上传的文件存储在非Web可访问的目录或对象存储中。
