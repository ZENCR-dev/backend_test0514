# 📢 前端团队通报 - 端口规范重大变更

**发布日期**: 2025年6月25日  
**发布人**: 后端开发团队  
**优先级**: 🔴 **紧急** - 立即生效

---

## 🚨 重要变更通知

为了规范开发环境并彻底解决跨域问题，项目已实施**端口规范统一变更**。请所有前端开发人员立即更新配置。

### 📋 新端口规范

| 服务类型 | 旧端口 | 新端口 | 说明 |
|---------|--------|---------|------|
| **前端服务** | 3000 | **3000-3009** | 3000（默认） |
| **后端API服务** | 3001 | **4000** | 主API服务 |
| **WebSocket服务** | 3001 | **4001** | 实时通信 |

### ⚡ 立即行动项

1. **更新API基础地址**：
   ```javascript
   // 旧配置 ❌
   const API_BASE_URL = 'http://localhost:3001/api/v1';
   
   // 新配置 ✅
   const API_BASE_URL = 'http://localhost:4000/api/v1';
   ```

2. **更新WebSocket连接**：
   ```javascript
   // 旧配置 ❌
   const socket = io('ws://localhost:3001', {
     path: '/ws/orchestration'
   });
   
   // 新配置 ✅
   const socket = io('ws://localhost:4001', {
     path: '/ws/orchestration'
   });
   ```

3. **更新环境变量**：
   ```bash
   # .env 或 .env.local
   NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
   NEXT_PUBLIC_WS_URL=http://localhost:4001
   NEXT_PUBLIC_WS_PATH=/ws/orchestration
   ```

### 📌 关键变更点

1. **API接口地址**（端口4000）：
   - Swagger文档：http://localhost:4000/api/docs
   - 健康检查：http://localhost:4000/api/v1/health
   - 药品API：http://localhost:4000/api/v1/medicines
   - 认证API：http://localhost:4000/api/v1/auth

2. **WebSocket端点**（端口4001）：
   - 业务编排：ws://localhost:4001/ws/orchestration
   - 注意：WebSocket使用独立端口4001，与API端口4000分离

3. **CORS已配置**：
   - 后端已配置允许3000-3009所有端口访问
   - 无需担心跨域问题

### 🛠 配置示例

**axios配置更新**：
```javascript
import axios from 'axios';

const apiClient = axios.create({
  baseURL: 'http://localhost:4000/api/v1', // API端口4000
  headers: {
    'Content-Type': 'application/json',
  },
  withCredentials: true,
});
```

**fetch API示例**：
```javascript
// 获取药品列表
const response = await fetch('http://localhost:4000/api/v1/medicines?limit=10');
const data = await response.json();
```

### ✅ 验证清单

请按以下步骤验证配置更新：

- [ ] 更新所有API_BASE_URL引用为4000端口
- [ ] 更新WebSocket连接地址为4001端口
- [ ] 更新环境变量文件（API使用4000端口，WebSocket使用4001端口）
- [ ] 清除浏览器缓存
- [ ] 重启前端开发服务器
- [ ] 测试API连接：访问 http://localhost:4000/api/v1/medicines
- [ ] 测试WebSocket连接：ws://localhost:4001/ws/orchestration

### 🔍 故障排查

如果遇到连接问题：

1. **检查后端服务状态**：
   ```bash
   # 检查API服务（端口4000）
   curl http://localhost:4000/api/v1/health
   
   # 检查端口占用情况
   # Windows
   netstat -ano | findstr :4000
   netstat -ano | findstr :4001
   
   # Mac/Linux
   lsof -i :4000
   lsof -i :4001
   ```

2. **WebSocket连接测试**：
   ```bash
   # 使用wscat测试WebSocket连接
   npx wscat -c ws://localhost:4001/ws/orchestration
   
   # 或使用浏览器开发者工具Network标签页检查WebSocket连接
   ```

3. **常见错误及解决**：
   - `ECONNREFUSED 4000`：API服务未启动，联系后端团队
   - `ECONNREFUSED 4001`：WebSocket服务未启动，联系后端团队
   - `CORS错误`：清除浏览器缓存，确认使用正确端口（API:4000, WS:4001）
   - `404错误`：检查API路径是否正确（注意v1版本前缀）
   - `WebSocket连接失败`：确认使用4001端口，检查路径/ws/orchestration
   - `混合端口错误`：确认API使用4000，WebSocket使用4001，不要混用

4. **端口冲突处理**：
   ```bash
   # Windows - 释放端口
   taskkill /PID <进程ID> /F
   
   # Mac/Linux - 释放端口
   kill -9 <进程ID>
   
   # PowerShell - 停止所有Node进程
   Get-Process | Where-Object { $_.ProcessName -eq "node" } | Stop-Process -Force
   ```

### 📞 技术支持

- **紧急问题**：项目群内 @后端开发团队
- **端口占用**：前端可使用3001-3009任意端口
- **API文档**：http://localhost:4000/api/docs
- **WebSocket测试**：ws://localhost:4001/ws/orchestration
- **服务健康检查**：http://localhost:4000/api/v1/health

### 🎯 影响范围

此次变更影响：
- ✅ 所有API调用
- ✅ WebSocket连接
- ✅ 开发环境配置
- ✅ 本地测试脚本

### 📅 实施时间

**立即生效** - 请在看到本通报后立即更新配置

---

## 🔥 快速启动指南

```bash
# 1. 更新代码中的端口配置（见上述说明）

# 2. 重启前端服务
npm run dev  # 或 yarn dev

# 3. 验证连接
# 浏览器访问：http://localhost:3000
# API应指向：http://localhost:4000/api/v1
# WebSocket应指向：ws://localhost:4001/ws/orchestration
```

---

**备注**：此次变更为永久性调整，已写入项目规范文档。今后所有开发必须遵循新的端口规范。

**感谢配合！** 🙏 